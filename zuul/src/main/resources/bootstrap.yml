spring:
  application:
    name: zuul
server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

ribbon:
  okhttp:
    enabled: true

zuul:
  prefix: /api
  strip-prefix: true


