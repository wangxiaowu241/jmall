spring:
  application:
    name: auth-server

server:
  port: 7777
  servlet:
    context-path: /uaa

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  #配置eureka 默认的地址，也可以分region配置eureka

  instance:
    prefer-ip-address: true